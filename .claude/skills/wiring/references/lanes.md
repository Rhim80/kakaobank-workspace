# 길 — 자료가 이 PC로 들어오는 세 가지 방법

같은 사람이라도 PC 상태에 따라 배선도가 달라진다. 위키 MCP가 붙어 있으면 스킬이 문서를 직접 읽고, 안 붙어 있으면 본인이 문서를 원본함에 넣는다. 그래서 길 확인은 줄기 표 다음, 깔기 전이다.

| 길 | 뜻 | 판정 근거 |
|---|---|---|
| **MCP** | Claude Code가 그 시스템에 직접 연결돼 있다 | `claude mcp list`에 `✔ Connected`이고, **그 서버의 읽기 도구로 한 건 가져와 `00-inbox/raw/`에 떨어졌다** |
| **CLI** | 터미널 명령으로 가져올 수 있다 | `command -v <도구>`가 경로를 내고 인증이 돼 있고(gws는 `gws auth status`), **명령 한 줄로 한 건 가져와 떨어졌다** |
| **직접 넣기** | 위 둘이 없다. 본인이 파일을 `00-inbox/raw/<길>/`에 넣는다 (내려받기·복사·붙여넣기) | 위 둘이 아니면 전부 여기. 실패가 아니라 길의 하나다 |

`scripts/check_lanes.sh`는 연결 여부까지만 찍는다. **연결됐다는 줄은 근거가 아니다** — 연결돼도 그 서버에 읽기 도구가 없을 수 있고, 기간을 못 줄 수도 있다. 근거는 실제로 한 건 가져와 떨어진 파일 경로이고, 그 호출을 선언표의 "된 호출" 칸에 그대로 적어야 다음에 수집 스킬이 반복한다. 스크립트는 판단하지 않는다 — "이 시스템은 MCP"라고 적는 것은 가져온 파일을 본 사람과 Claude다.

**0건과 안 돌았음을 가른다.** `claude mcp list`가 비어 있으면 스크립트가 "목록이 비었는지 명령이 실패했는지 확인"이라고 찍는다. 그 줄이 나오면 종료 코드와 출력을 보고 판정한다. 연결 0개인 PC도 정상이다 — 모든 길이 "직접 넣기"인 배선도가 그려진다. 단 0개로 판정하기 전에 스크립트의 「이 폴더에서는 안 켜지는 MCP」 절을 본다 — 다른 폴더·오픈코드에만 등록된 MCP는 `claude mcp list`에 안 나온다.

**`Needs authentication`·`Pending approval`은 MCP가 아니다.** 목록에 있어도 인증이 안 됐으면 그 자리에서 인증을 마치거나(참가자 몫), 오늘은 "직접 넣기"로 적는다. 배선도는 지금 되는 길만 적는다.

---

## 카카오뱅크 사내 시스템 — 미리 조사된 연결 길 (2026-07-31 조사, 공개 자료 기준)

카카오뱅크가 "연동 가능"으로 알려준 11종이다. 아래는 공개 자료로 조사한 것이라 **사내 실제 상태와 다를 수 있다.** 길 확인의 근거는 이 표가 아니라 본인 PC의 `check_lanes` 출력이다. 이 표는 "이 시스템은 어떤 길이 있을 수 있나"를 미리 알고 물어보기 위한 것이다.

| 시스템 | 있을 수 있는 길 | 메모 |
|---|---|---|
| Google Workspace (시트·드라이브·메일) | CLI: `gws` | 관리자가 OAuth 스코프 단위로 허용·차단한다. `gws auth status`로 확인 |
| Wiki (Confluence) | MCP (사내 서버) | 자체 호스팅이면 공식 Atlassian MCP는 안 붙는다. 사내에 띄운 MCP 서버 이름을 `check_lanes` 출력(`claude mcp list`와 「이 폴더에서는 안 켜지는 MCP」 절 둘 다)에서 찾는다 |
| Atlassian (Jira) | MCP (사내 서버) | 위와 같은 구조 |
| GitLab | MCP | 공식 MCP 서버 있음(자체 호스팅은 18.7 이상 + 유료 등급) |
| Figma | MCP | 데스크톱 로컬판(`127.0.0.1:3845`)은 PC 안에서 통신이 끝난다 |
| Mattermost (메신저) | MCP 또는 REST | 커뮤니티 MCP 서버 여럿. 응답에 적힌 "코코"가 이것인지는 본인에게 묻는다 |
| Apps Script | CLI: `clasp` | 로컬에서 코드를 쓰고 올리는 구조 |
| Agit (사내 게시판) | 확인 안 됨 | 공개 API 없음. 없으면 "직접 넣기" |
| Start카뱅 (그룹웨어) · BI포털 · Second Brain (LLM wiki) | 확인 안 됨 | 카카오뱅크 자체 시스템. `claude mcp list`에 `✔ Connected`면 MCP. 거기 없어도 「이 폴더에서는 안 켜지는 MCP」 절에 있으면 다시 등록할지 본인에게 묻는다. 둘 다 없으면 "직접 넣기" |

응답에 자주 나온 채널 중 11종 밖의 것: 메일 · 회의(구두) · 모바일 앱 동작 확인 · 엑셀 · 한글 · 외부 웹사이트. 회의·구두는 도구가 아니라 사람 채널이다 — 길은 "직접 넣기"(회의록·메모를 원본함에)이고, 그렇게 적는다.
